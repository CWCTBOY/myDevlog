import { BrowserRouter, Routes } from "react-router-dom";

function Router() {
  return (
    <BrowserRouter>
      {/*The global components area*/}
      <Routes>
      </Routes>
    </BrowserRouter>
  )
}
export default Router;